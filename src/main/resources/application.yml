spring:
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
  security:
    oauth2:
      client:
        registration:
          keycloak:
            client-id: keycloak
            client-secret: cFhP4SsIoOSi02GRug8UWbeoKCvYa9UX
            scope: openid,profile,roles
            authorization-grant-type: authorization_code
            redirect-uri: http://localhost:8088/login/oauth2/code/keycloak
            provider: keycloak
        provider:
          keycloak:
            issuer-uri: http://localhost:8084/realms/CinePass
  datasource:
    url: jdbc:mysql://localhost:3306/cinepass?useSSL=false&serverTimezone=UTC
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: password
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true
        open-in-view: false
    show-sql: false
    open-in-view: false

server:
  port: 8088

api:
  prefix: /api/v1